"""ETL utilities for processed parquet marts."""

# Intentionally lightweight — import submodules directly:
#   python -m src.etl.build_cache
#   from src.etl.build_cache import build_cache, cache_is_valid
